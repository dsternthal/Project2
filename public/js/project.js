const markComplete = async (event) => {
    const completeEl = document.querySelector('#complete')
    if(event.target.hasAttribute('#importance')) {
        
    }

}


document
.querySelector('#complete')
.addEventListener('click', markComplete);